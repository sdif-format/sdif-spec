@sdif 1.0
kind Plan
id agent.context.plan
title "Agent context plan"
steps[id,status,owner]:
  S1	ready	agent.scheduler
  S2	blocked	agent.executor
rel:
  S2 depends_on S1
rules:
  (deny missing(owner))
intent """
Keep compact context directly in SDIF.
"""
